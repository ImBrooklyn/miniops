package kube

const (
    conferDir       = "/app/confer"
    configmapDir    = "configmap"
    secretDir       = "secret"
    resourceDir     = "resources"
    configmapVolume = "configmap-volume"
    secretVolume    = "secret-volume"
    resourceVolume  = "resource-volume"
)
